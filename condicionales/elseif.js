var cantidadSolicitada = 14;
if (cantidadSolicitada > 15) {
    console.log('Aplicariamos un descuento grande');
}
else if (cantidadSolicitada > 10 && cantidadSolicitada < 15) {
    console.log('Aplicamos un decuento pequeño');
}
else {
    console.log('No hacer nada');
}
